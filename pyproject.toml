[project]
name = "cam_streamer"
version = "0.1.0"
description = "An easy way to get a low-cost video surveillance system"
readme = "README.md"
requires-python = ">=3.14.4"
dependencies = [
    "config<0.5",
    "glob2>=0.7",
    "psutil>=7.2.2",
    "python-daemon>=3.1.2",
    "python-strtobool>=1.0.3",
    "schedule>=1.2.2",
    "sortedcontainers>=2.4.0",
]
